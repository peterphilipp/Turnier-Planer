version: '3.8'
services:
  app:
    image: ghcr.io/peterphilipp/turnier-planer:latest
    ports:
      - "5000:5000"
    environment:
      DATABASE_URL: "file:./data/dev.db"
    volumes:
      - db-data:/app/data

volumes:
  db-data:
